# Zero-controller / zero-route component export.
# Vendoring no longer exports Symfony controller routes.
# Host routing is owned by Cruding catch-all grammar and App\Vendoring\Service\Http\* FQCN convention.
# Route-map registry files live under config/platform/routes/ and are not native Symfony routes.
