doctrine:
    dbal:
        url: '%env(default:vendor.dsn:VENDOR_DSN)%'
    orm:
        validate_xml_mapping: false
        mappings:
            Vendoring:
                type: attribute
                dir: '%kernel.project_dir%/src/Entity'
                prefix: 'App\Vendoring\Entity'
                is_bundle: false
