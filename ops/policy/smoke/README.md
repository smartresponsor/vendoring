# Vendoring policy smoke scripts

This directory contains operational smoke scripts for the policy configuration stored under `ops/policy/config/`.

The scripts are intentionally kept out of the repository root so that the root remains reserved for application entrypoints and project metadata.
