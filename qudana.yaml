version: "1.0"

profile:
  path: .qodana/inspection-profile.xml

exclude:
  - name: All
    paths:
      - vendor/**
      - node_modules/**
      - var/**
      - archive/**
      - .git/**
      - public/build/**

failureConditions:
  severityThresholds:
    critical: 0
    high: 0