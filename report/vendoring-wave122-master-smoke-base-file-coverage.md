# Vendoring Wave 122

Expanded `tests/bin/smoke.php` so master smoke explicitly requires physical presence of base-layer smoke files and their paired Composer scripts for `symfony-stack`, `di`, `entity`, `root-runtime-artifacts`, `transaction-doctrine`, and `transaction-status-persistence`.
