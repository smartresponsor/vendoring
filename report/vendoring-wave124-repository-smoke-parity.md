# Vendoring Wave 124 Report

- add `tests/bin/repository-contract-smoke.php`
- normalize `test:repository` to canonical `smoke + phpunit` pattern
- extend master `tests/bin/smoke.php` to require repository smoke file presence
