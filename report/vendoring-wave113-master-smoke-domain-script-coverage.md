# Vendoring Wave 113

- Extended master smoke to require domain orchestration scripts `test:statement`, `test:payout`, and `test:controller`.
- This closes the gap where those scripts existed in composer.json but were not enforced by top-level smoke.
