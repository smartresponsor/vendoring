# RC Evidence Pack

Generated through `composer docs:rc-evidence` and validated by `composer test:rc-evidence`.


The release evidence layer also includes `composer docs:release-manifest`, which writes `release-manifest.*` and `rollback-manifest.*` artifacts under `build/release/`.
