{
  "proposal": [
    {
      "op": "agent.required",
      "level": "error",
      "scope": [
        "."
      ],
      "note": "root-contract violations need reasoning",
      "prompt": "Root-contract violation detected (Vendoring repo).\nRequired root files are .gitignore, README.md and composer.json. Allowed application/tooling directories are bin, build, config, delivery, deploy, docs, drivers, migrations, ops, public and src. Allowed dot-folders are .canonization, .commanding, .consuming, .gate, .github, .ide and .intelligence.\nDo NOT recreate MANIFEST.json at root and do not delete/move blindly.\nOutput: PR-ready patch + updated gate contract/docs if needed."
    }
  ]
}
